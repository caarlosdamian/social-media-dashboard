export interface FollowerInfo {
  id: number;
  username: string;
  totalAmount: string;
  todayAmount: string;
  icon: string;
  up: boolean;
  color: string;
}

export interface StatsInfo {
  id: number;
  header: string;
  totalAmount: string;
  todayAmount: string;
  icon: string;
  up: boolean;
}
